requests

# Pinned to a 4.x release: OpenCV 5.0 dropped cv2.CascadeClassifier from the
# core module, which anpr_engine.py's Haar-cascade plate detector needs.
# RapidOCR declares opencv-python as a dependency, so use this package name
# and keep all OpenCV variants pinned to one installation.
opencv-python==4.10.0.84

# Only needed by anpr_engine_easyocr.py (lazily imported).
easyocr

# Only needed by anpr_engine_rapidocr.py (lazily imported).
rapidocr_onnxruntime==1.2.3

# Only needed by anpr_engine_roboflow.py.
inference-sdk
python-dotenv

# Only needed for test_accuracy_report.ipynb.
pandas
matplotlib
seaborn
jupyter
